name: Facebook

on:
  workflow_call:
  workflow_dispatch:

jobs:
  facebook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Facebook repository
        uses: actions/checkout@v5
        with:
          repository: "${{ github.repository_owner }}/FacebookEvents-DiscordWebhook"
          path: "${{ github.workspace }}/"
          token: "${{ secrets.PAT }}"

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: |
          pip install -r ${{ github.workspace }}/requirements.txt

      - name: Run Facebook script
        run: |
          python ${{ github.workspace }}/Facebook.py