name: Monta

on:
  workflow_call:
  workflow_dispatch:

jobs:
  monta:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Monta repository
        uses: actions/checkout@v5
        with:
          repository: "${{ github.repository_owner }}/Monta-price-checker"
          path: "${{ github.workspace }}/"
          token: "${{ secrets.PAT }}"

      - name: Install Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: |
          pip install -r ${{ github.workspace }}/requirements.txt

      - name: Run Monta script
        run: |
          python ${{ github.workspace }}/Monta.py